# Ejemplo de variables de entorno para el servidor de envío de correos
# Rellena con tus datos reales antes de ejecutar o desplegar el servicio.

# SMTP (obligatorio)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=usuario@example.com
SMTP_PASS=secreto

# Dirección "From" que aparecerá en los correos (opcional — por defecto se usa SMTP_USER)
FROM_ADDRESS=no-reply@example.com

# Correo del administrador/propietario que recibirá copia de cada cotización (obligatorio)
ADMIN_EMAIL=admin@example.com

# Puerto donde correrá el servicio (opcional)
PORT=3000

# Opcional: CORS y rate limiting para entornos de staging/producción
# Orígenes permitidos (coma-separados), dejar vacío para no aplicar CORS desde aquí
CORS_ORIGINS=
# Habilitar limitador de peticiones (1 para activar)
ENABLE_RATE_LIMIT=0
# Ventana en ms (por defecto 15 minutos)
RATE_LIMIT_WINDOW_MS=900000
# Máximo de peticiones por ventana (por defecto 100)
RATE_LIMIT_MAX=100
# Límite del body parser (por ejemplo 25mb)
BODY_LIMIT=25mb
